[project]
name = "example-course"
version = "0.1.0"
description = "An example course integrated as a uv workspace member"
dependencies = [
    "ipython",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
