# 2. Installation

Install LiteLLM using `uv`. Open Terminal and run:

```bash
uv tool install litellm
```

To confirm it installed correctly, run:

```bash
litellm --version
```
(You should see a version number output).

**Next:** [3. Configuration](3-configuration.md)
